CREATE TABLE IF NOT EXISTS tasks (id uuid PRIMARY KEY, title VARCHAR(70) NOT NULL, description VARCHAR(255) NULL, user_id uuid NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, CONSTRAINT fk_tasks_user_id__id FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE ON UPDATE RESTRICT);
